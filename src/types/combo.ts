export interface Combo {
  id: string;
  name: string;
  icon: string;
  iconColor: string;
  borderColor: string;
  discount: string;
  discountColor: string;
  items: string[];
  productIds?: string[];
  price: number;
  originalPrice: number;
  buttonStyle: string;
}

